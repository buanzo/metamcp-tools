[servers.fetch]
enabled = false
template = true
description = "Fetch MCP template for HTTP retrieval. Review network policy before enabling."
command = "uvx"
args = ["mcp-server-fetch"]
startup_timeout_sec = 30
tool_timeout_sec = 60

