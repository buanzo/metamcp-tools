[servers.filesystem_readonly]
enabled = false
template = true
description = "Filesystem MCP template scoped to a local directory. Replace /path/to/root before enabling."
command = "npx"
args = ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/root"]
startup_timeout_sec = 30
tool_timeout_sec = 60

